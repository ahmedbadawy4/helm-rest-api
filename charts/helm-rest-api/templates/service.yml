apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-rest-api.fullname" . }}
  labels:
    {{- include "helm-rest-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "helm-rest-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
